AWSTemplateFormatVersion: "2010-09-09"
Description: Hark public Lambda application with provider-resilient reasoning and bounded SSM access.

Parameters:
  CodeBucket:
    Type: String
  CodeKey:
    Type: String
  ConfigParameterName:
    Type: String
    Default: /hark/prod/database
  ProviderParameterName:
    Type: String
    Default: /hark/prod/providers
  KillSwitchParameterName:
    Type: String
    Default: /hark/prod/execution-enabled
  BedrockModelId:
    Type: String
    Default: amazon.nova-micro-v1:0
  GeminiPrimaryModelId:
    Type: String
    Default: gemini-3.5-flash-lite
  GeminiTertiaryModelId:
    Type: String
    Default: gemini-3.1-flash-lite
  GeminiEmbeddingModelId:
    Type: String
    Default: gemini-embedding-2

Resources:
  HarkExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: hark-prod-lambda-role
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - !Sub arn:${AWS::Partition}:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: HarkRuntimeLeastPrivilege
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: ReadOnlyHarkParameters
                Effect: Allow
                Action: ssm:GetParameter
                Resource:
                  - !Sub arn:${AWS::Partition}:ssm:${AWS::Region}:${AWS::AccountId}:parameter/hark/prod/database
                  - !Sub arn:${AWS::Partition}:ssm:${AWS::Region}:${AWS::AccountId}:parameter/hark/prod/providers
                  - !Sub arn:${AWS::Partition}:ssm:${AWS::Region}:${AWS::AccountId}:parameter/hark/prod/execution-enabled
              - Sid: BedrockAvailability
                Effect: Allow
                Action: bedrock:GetFoundationModelAvailability
                Resource: "*"
              - Sid: BoundedBedrockInference
                Effect: Allow
                Action: bedrock:InvokeModel
                Resource:
                  - !Sub arn:${AWS::Partition}:bedrock:${AWS::Region}::foundation-model/${BedrockModelId}

  HarkFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: hark-prod
      Description: Execution memory for Agent Skills.
      Runtime: python3.12
      Architectures: [x86_64]
      Handler: hark.web.lambda_handler
      Role: !GetAtt HarkExecutionRole.Arn
      Code:
        S3Bucket: !Ref CodeBucket
        S3Key: !Ref CodeKey
      MemorySize: 512
      Timeout: 90
      Environment:
        Variables:
          HARK_DATABASE_PARAMETER: !Ref ConfigParameterName
          HARK_PROVIDER_PARAMETER: !Ref ProviderParameterName
          HARK_KILL_SWITCH_PARAMETER: !Ref KillSwitchParameterName
          BEDROCK_MODEL_ID: !Ref BedrockModelId
          GEMINI_PRIMARY_MODEL_ID: !Ref GeminiPrimaryModelId
          GEMINI_TERTIARY_MODEL_ID: !Ref GeminiTertiaryModelId
          GEMINI_EMBEDDING_MODEL_ID: !Ref GeminiEmbeddingModelId
          EMBEDDING_DIMENSIONS: "256"
          MEMORY_SIMILARITY_THRESHOLD: "0.73"
          BEDROCK_HEALTH_TTL_SECONDS: "300"
          HARK_GEMINI_35_REQUEST_BUDGET_PER_DAY: "200"
          HARK_GEMINI_31_REQUEST_BUDGET_PER_DAY: "100"
          HARK_GEMINI_EMBEDDING_REQUEST_BUDGET_PER_DAY: "150"
          HARK_GEMINI_35_REQUEST_LIMIT_PER_MINUTE: "12"
          HARK_GEMINI_31_REQUEST_LIMIT_PER_MINUTE: "12"
          HARK_GEMINI_EMBEDDING_REQUEST_LIMIT_PER_MINUTE: "90"
          DEMO_RUN_LIMIT_PER_DAY: "4"
          GLOBAL_RUN_LIMIT_PER_DAY: "40"
          GLOBAL_TOTAL_RUN_LIMIT: "1000"
          MAX_CONCURRENT_RUNS: "3"
          MAX_AGENT_ITERATIONS: "5"
          RUN_TIMEOUT_SECONDS: "60"
          DEMO_RETENTION_DAYS: "45"
          HARK_ENVIRONMENT_ID: restricted-orders-gemini-embedding-2-256-v1
          EXECUTION_ENABLED: "true"
      Tags:
        - Key: Project
          Value: Hark
        - Key: Environment
          Value: production

  HarkFunctionUrl:
    Type: AWS::Lambda::Url
    Properties:
      AuthType: NONE
      InvokeMode: BUFFERED
      TargetFunctionArn: !GetAtt HarkFunction.Arn

  PublicFunctionUrlPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunctionUrl
      FunctionName: !Ref HarkFunction
      FunctionUrlAuthType: NONE
      Principal: "*"

  PublicFunctionInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref HarkFunction
      InvokedViaFunctionUrl: true
      Principal: "*"

Outputs:
  PublicUrl:
    Description: Public Hark website.
    Value: !GetAtt HarkFunctionUrl.FunctionUrl
  FunctionName:
    Value: !Ref HarkFunction
